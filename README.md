# 每日任务（Daily Tasks）

一个 Obsidian 插件，右侧边栏任务管理工具 — 时钟/倒计时/日历/待办清单 + DDL + 时间轴。

## 功能

### 📁 日记归档
1. **自动创建今日日记** — 每次打开 Obsidian 时，若今日日记不存在则自动创建（支持自定义模板）
2. **自动归档旧文件** — 日记文件夹只保留最近 N 个文件，更早的自动移至 `历史记录/` 子文件夹

### 📺 右侧边栏
3. **🕐 实时时钟 + 下班倒计时** — 显示当前时间（时:分:秒），距下班时间倒计时，过点自动切到明天；点击倒计时文字可切换隐藏/显示
4. **📅 日历** — 月视图，可切换上/下月，有日记的日期有下划线标记，点击直接打开当日日记
5. **📋 未完成任务清单** — 扫描指定文件夹下所有笔记中的 `- [ ]` 未勾选待办，支持：
   - ☑ 勾选完成（实时写入源文件）
   - 单击任务文本 → 跳转到对应笔记并定位到该行
   - 自动刷新（文件变更时同步更新）
   - 📅 **设置截止日** — 点击任务旁的 DDL 图标弹出日期选择器，选中的日期写入原笔记
   - 点击 DDL 文字重复设置可更新截止日
   - ⊘ **取消标记** — 点击「取消」按钮为任务添加 ~~删除线~~，已取消任务不会出现在待办列表，也不会被继承到新日记
   - 🔄 **自动继承未完成任务** — 创建新日记时，自动将前几天未完成且未取消的任务添加到今日（可开关）

### ⚙️ 完整设置面板
全部选项可在前台配置，**修改实时生效**，无需重启 Obsidian。

## 安装

1. 将整个 `daily-note-archiver` 文件夹放入你 vault 的 `.obsidian/plugins/` 目录
2. 打开 Obsidian → **设置** → **第三方插件** → **关闭安全模式**
3. 在「已安装插件」列表中找到 **每日笔记归档器**，启用即可

## 配置方式

安装并启用后，打开 **设置 → 第三方插件 → 每日笔记归档器**，所有选项即改即生效。

## 目录结构示例

```
库根目录/
├── 日记/
│   └── 每日/
│       ├── 2025-03-27.md   ← 最近 N 个保留在此
│       ├── 2025-03-26.md
│       └── 历史记录/        ← 超出 N 的自动移到这里
│           ├── 2025-03-20.md
│           └── ...
└── .obsidian/
    └── plugins/
        └── daily-note-archiver/
            ├── manifest.json
            ├── main.js
            ├── styles.css
            └── README.md
```

## 设置项说明

### 📁 归档路径

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 日记文件夹路径 | 文本框 | 存放日记的目录，相对于库根目录 |
| 历史记录文件夹路径 | 文本框 | 旧日记移入的目录 |

### 📌 保留规则

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 保留文件数 | 滑块（1-60） | 当前文件夹保留多少个文件 |

### 📅 日期 & 模板

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 日期格式 | 文本框 | moment.js 格式（`YYYY-MM-DD` / `YYYYMMDD` 等） |
| 标题行 | 文本框 | 文件首行，支持 `{{date}}` 占位符 |
| 正文模板 | 多行文本框 | 标题之后的内容，支持 `{{date}}` 占位符 |

### ⚙️ 归档行为

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 自动创建今日日记 | 开关 | 启动时自动创建 |
| 自动归档旧文件 | 开关 | 启动时自动归档 |
| 立即执行一次 | 按钮 | 手动触发归档 |

### 📺 侧边栏

| 设置项 | 类型 | 说明 |
|--------|------|------|
| 下班时间 | 文本框 | HH:mm 格式，用于倒计时 |
| 待办扫描文件夹 | 文本框 | 扫描此文件夹下所有笔记中的未完成任务 |
| 显示时钟 & 倒计时 | 开关 | 是否显示时钟模块 |
| 显示日历 | 开关 | 是否显示日历模块 |
| 显示未完成任务 | 开关 | 是否显示待办列表模块 |

## 手动触发

打开设置面板点击「**立即执行**」按钮，或在控制台执行：

```js
app.plugins.plugins["daily-note-archiver"].runNow()
```

## 从旧版迁移

如果你之前直接修改过 `main.js` 中的默认值，安装新版后请通过设置面板重新配置——设置面板的修改会存入 `data.json`，优先级高于代码中的默认值。

## 更新记录

### v1.4.1

- 🐛 修复：插件加载失败（ArchiverSettingTab 类被误删）
- 🐛 修复：时间轴只显示最近 30 条记录

### v1.4.0

- ✨ **完成时间戳** — 勾选待办时自动标注完成时间 ✅ HH:mm，取消勾选自动清除
- ✨ **任务时间轴** — 点击 📊 时间轴 查看任务在所有日记中的完整轨迹，支持跳转
- ✨ **双向取消标记** — 点击 ⊘ 取消添加 strikethrough，再次点击恢复

### v1.3.0

- ✨ **取消标记** — 点击 ⊘ 取消按钮为任务添加 ~~删除线~~，已取消任务不展示、不继承
- ✨ **任务自动继承** — 创建新日记时自动附带前几天未完成且未取消的任务，可前台开关
- ✨ **智能去重** — 相同任务文本跨文件只显示一次，勾选后不会从旧笔记中再次冒出

### v1.2.0

- ✨ **待办 DDL** — 点击任务旁的 `📅` 弹出日期选择器，截止日写入原笔记
- ✨ **倒计时一键隐藏** — 点击倒计时文字可切换隐藏/显示，时钟保持可见
- 🐛 **日历打开历史日记** — 点击日期同时查找 `每日/` 和 `历史记录/`

### v1.1.0

- ✨ **右侧边栏** — 新增侧边栏视图（时钟/倒计时/日历/待办清单）
- ✨ **实时时钟** — 显示当前时间（时:分:秒），每秒刷新
- ✨ **下班倒计时** — 距配置的下班时间实时倒计时，过点自动切到明天
- ✨ **日历** — 月视图，可切换上/下月，点击「今天」回到当月；有日记的日期有下划线标记
- ✨ **待办清单** — 扫描指定文件夹下所有笔记中的 `- [ ]` 未完成任务，支持勾选、跳转
- ✨ **设置实时生效** — 修改设置后侧边栏立即刷新，无需重启 Obsidian
- 🐛 **日历末行对齐** — 最后一行不满 7 格时补齐空白格
- 🐛 **待办防重入** — 修复勾选后显示两行空状态的并发问题
- 🐛 **日历打开历史日记** — 点击日期同时查找 `每日/` 和 `历史记录/`

### v1.0.0

- ✨ 初始版本：自动创建今日日记 + 归档旧文件
- ✨ 完整设置面板（路径、保留数、日期格式、日记模板）
- ✨ 设置面板切换到 GitHub 友好（脱敏默认路径）

## 许可证

MIT
